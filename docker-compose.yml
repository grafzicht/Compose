version: '3'
services:
  proxy:
    build: ./proxy
    ports:
      - "80:80"

  zookeeper:
    image: "zookeeper"

  kafka:
    image: wurstmeister/kafka
    ports:
      - "9092:9092"
    depends_on:
      - zookeeper

  #TODO add IsHome
  #TODO add Monitoring