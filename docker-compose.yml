version: '3'
services:
  proxy:
    build: ./proxy
    container_name: proxy
    ports:
      - "80:80"

  influx:
    image: influxdb:1.7
    environment:
      INFLUXDB_DB: grafzicht
    ports:
      - "8086:8086"


  #TODO add zookeeper
  #TODO add kafka
  #TODO add IsHome
  #TODO add Monitoring
  #TODO add AMQP server
  #TODO add Boodschappenlijstje
